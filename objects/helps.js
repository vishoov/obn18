// Object.keys(obj)


const user = {
    name:"Roopansh",
    age:100,
    isAdmin:false,
    address:123
}

const keys = Object.keys(user)

console.log(keys)


const values = Object.values(user)
console.log(values)